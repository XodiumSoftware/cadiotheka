//! Main hub UI component shown after a successful login.

/// State for the hub UI.
#[derive(Default)]
pub struct Hub;

impl Hub {
    /// Renders the hub UI.
    pub fn show(&mut self, _ui: &mut egui::Ui) {
        // TODO: add hub content here
    }
}
