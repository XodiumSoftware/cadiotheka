[package]
name = "cadiotheka"
version = "0.0.1"
edition = "2024"
authors = ["Xodium"]
description = "The open hub for CAD creators"

[dependencies]
egui = "0.35.0"
eframe = "0.35.0"

[profile.release]
lto = true
strip = true

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
