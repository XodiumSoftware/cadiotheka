1. Fix tag/platform overflow on project cards where the `+<int>` on hover is not showing the remaining tags/platforms.
2. Fix the 3D viewer fullscreen showing an empty project modal instead of the 3D viewer only.
3. 3D-viewer controls do not work (pan/move)
4. Profile button in the header doesnt show tooltip even when defined.
5. Load an HDR skybox for image-based lighting (IBL) on `PhysicalMaterial`.
6. Move the plaforms and tags to the database.
7. Adjust profile dropdown for admin's to have a AdminModal where the admin can remove/create/rename platforms/tags.
8. Rename the subprojects to remove the prefix.
