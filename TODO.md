1. Fix tag/platform overflow on project cards where the `+<int>` on hover is not showing the remaining tags/platforms.
2. Profile button in the header doesnt show tooltip even when defined.
3. Load an HDR skybox for image-based lighting (IBL) on `PhysicalMaterial`.
4. Adjust profile dropdown for admin's to have a AdminModal where the admin can remove/create/rename platforms/tags.
5. Rename the subprojects to remove the prefix.
